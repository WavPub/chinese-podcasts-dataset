/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: "/china-podcast-tiny-web",
    images: {
        unoptimized: true,
    },

};

export default nextConfig;
