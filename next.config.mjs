/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: "/nextjs-github-pages",
    images: {
        unoptimized: true,
    },

};

export default nextConfig;
